# Pinned tooling for the SBOM compliance gate (see .github/workflows/static_checks.yml).
# Pinning a reviewed version keeps the CI check reproducible: an upstream
# release cannot silently change or break the gate without a change here.
check-jsonschema==0.37.4
